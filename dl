#!/usr/bin/ruby

Dir.chdir(File.expand_path("~/Downloads")) do
  system "ruby sync.rb push"
  unless ARGV.empty? then
    curls = ARGV.map { |url| "curl -OL \\\"#{url}\\\"" }.join " && "
    system "ssh e \"sh -vxc 'cd ~/Downloads && #{curls}'\""
    system "ruby sync.rb pull"
  end
end
