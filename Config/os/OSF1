# -*-ksh-*-

OSPATH=/bin:/usr/local/bin:/usr/bin/X11:/usr/ucb:/sbin

case $- in
  
  # interactive shell
  *i*)
       alias gmake=make
       stty echoe
       stty discard undef
    ;;
  
  # not interactive
  *) 
    ;;
esac

